DATABASE_URL=
AUTH_SECRET=
CREDENTIALS_KEY=
OWNER_ACCESS_CODE=
EVE_SERVICE_TOKEN=
CRON_SECRET=
# Gateway uses Vercel OIDC on deployment; local tokens come from vercel env pull.
VERCEL_OIDC_TOKEN=
# Set after configuring a dedicated Photon connector.
PHOTON_CONNECTOR=
# Deployment kill switches. Production has owner-authorized execution; keep local/preview disabled.
ESPN_LINEUP_WRITES_ENABLED=false
ESPN_WAIVER_WRITES_ENABLED=false
ESPN_TRADE_WRITES_ENABLED=false
